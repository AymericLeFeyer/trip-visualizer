/**
 * Code déverrouillant le mode admin (édition).
 * ⚠️ Barrière côté client uniquement : empêche l'édition accidentelle par un
 * visiteur, ce n'est pas une authentification serveur.
 */
export const ADMIN_CODE = 'SRAPIX';
